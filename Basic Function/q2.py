#  Create a function that takes two numbers and returns their sum.
def sum_are(num1,num2):
    sum = num1 + num2
    return sum
print(sum_are(3,6))
print(sum_are(3,4))
print(sum_are(4,4))

# whenever we call function and give arguments we get different output on the basis of argument but the procedure remain same
